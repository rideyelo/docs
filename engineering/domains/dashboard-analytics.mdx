---
title: "Dashboard analytics internals"
description: "Exact SQL definitions, time windows, delta math, Redis reads, and background refresh behind the dashboard's KPI, live operations, driver ops, presence, telemetry, rides, search, and notification analytics endpoints."
---

The dashboard's reporting surfaces are computed on request from Postgres and Redis. Nothing is pre-aggregated except the GTM prospect snapshot and the five-minute competitor quote cache. This page documents what each number means in SQL, which window it covers, and how deltas are calculated. For which screen calls which endpoint and how often, see [Dashboard data map](/engineering/web/dashboard-data-map). For the product definitions, see [Ride metrics](/dashboard/ride-metrics). For the user directory, access management, and role scoping, see [Dashboard directory and scope](/engineering/domains/dashboard-directory-and-scope).

Paths are relative to `yelo-api/`.

## Data touched

| Store | Objects | Used by |
| --- | --- | --- |
| Postgres | `Rides`, `Users`, `UserAttribution`, `DriverApplications`, `Communities`, `EventAttendees` | KPI, live analytics, market KPIs |
| Postgres | `DriverSessions`, `FleetDrivers`, `Fleets`, `Vehicles`, `RideDeclineReasons` | Market cards, market drivers, driver ops |
| Postgres | `CommunityOperatingWindows` | Market operating hours |
| Postgres | `LocationSearches`, `LocationBrowses` | Search insights, market KPI searches |
| Postgres | `DeviceTelemetry` | Telemetry summary |
| Postgres | `AttributionTouches`, `referral_sources`, `AuthTransactions`, `Referrals` | Acquisition breakdown |
| Postgres | `KpiManualLog` | Manual KPI log |
| Postgres | `surge_history`, `Communities.surge_*` | Surge set and history |
| Postgres | `Notifications`, `NotificationCohorts`, `NotificationDeliveries` | Notification analytics |
| Redis | `presence:{userId}` hash, `community:online:{id}` and `community:drivers:{id}` sorted sets | Presence, market online counts, market driver status, user last seen |
| Redis | `community:warm:{id}` sorted set, `driver:loc:{userId}` hash | Warm pool counts and markers |
| Redis | `gtm:resolved`, `gtm:city:{lat}:{lng}` | GTM snapshot and reverse-geocode cache |
| Mapbox | `ReverseGeocodeCity` | GTM refresh and approvals city labels |
| Clerk | `user.Get` | Surge audit name and email |
| PostHog Query API | `ride_aggregator_prices_loaded` events | Observed competitor quotes on **Live** |

Presence and warm-pool mechanics are owned by [Heartbeat and presence](/engineering/location/heartbeat-and-presence) and [Activation tiers](/engineering/matching/activation-tiers). This page only covers how the dashboard aggregates them.

## Endpoints

All routes sit under the `/dashboard` group, which runs Clerk auth and `dashboardScopeAllows` first. "Admin (scope)" means the scope middleware rejects `intern` and `fleet` with a 401 before the handler runs. There is no `/dashboard/supply-demand` prefix. "No dashboard caller" means no routed `yelo-web` page calls the endpoint.

| Method | Path | Auth | Handler and purpose |
| --- | --- | --- | --- |
| GET | `/dashboard/kpi/global` | Admin (scope and `requireAdmin`) | `KpiHandler.getGlobalKPIs`: all-time totals, week-over-week deltas, 12-week series |
| GET | `/dashboard/kpi/acquisition` | Admin | `getAcquisition`: signup source breakdown |
| GET, POST | `/dashboard/kpi/manual` | Admin | `getManual`, `postManual`: manual revenue, spotlights, impressions log |
| GET | `/dashboard/kpi/approvals` | Admin | `getApprovals`: pending driver applications across communities |
| GET | `/dashboard/{id}/kpis?range=` | Admin, or intern for own community | `CommunityHandler.GetMarketKPIs` |
| GET | `/dashboard/community/{id}/cancellations/kpis`, `/cancellations`, `/cancellations/breakdowns` | Admin, or intern GET for own community | Matched-cancellation KPIs, table, breakdowns |
| GET | `/dashboard/community/{id}/uncompletes/kpis`, `/uncompletes`, `/uncompletes/breakdowns` | Same | Uncompleted-ride KPIs, table, breakdowns |
| GET | `/dashboard/{id}/search-insights`, `/search-map-data` | Admin, or intern for own community | Search conversion and map points |
| GET | `/dashboard/fleet/{fleet_id}/kpis`, `/histogram` | Admin, or fleet with `HasFleetAccess` | Fleet KPIs and hourly histogram |
| GET | `/dashboard/live/analytics?period=` | Admin (scope) | `SupplyDemandHandler.getAnalytics`: period KPIs, service timing, outcome counts, ride journey |
| GET | `/dashboard/live/competitor-prices?period=` | Admin (scope) | `getCompetitorPrices`: observed UberX and Lyft Standard quotes |
| GET | `/dashboard/live/kpis`, `/markets` | Admin (scope) | `getKPIs` (no dashboard caller), `getMarkets` |
| GET | `/dashboard/live/markets/{id}/drivers`, `/rides?older=` | Admin (scope) | Market drivers; market rides has no dashboard caller |
| POST | `/dashboard/live/markets/{id}/surge` | Admin (scope) | `setSurge` |
| GET | `/dashboard/live/markets/{id}/surge-history` | Admin (scope) | `getSurgeHistory` (no dashboard caller) |
| POST | `/dashboard/live/markets/{id}/notify` | Admin (scope) | `notifyMarket` (no dashboard caller) |
| POST | `/dashboard/live/drivers/{id}/ping`, `/riders/{id}/ping` | Admin (scope and `requireAdmin`) | `pingDriver`, `pingRider` (no dashboard caller) |
| GET | `/dashboard/driver-ops` | Admin (scope and `requireAdmin`) | `DriverOpsHandler.getSnapshot` (no dashboard caller) |
| GET | `/dashboard/presence/map` | Admin (scope) | `PresenceDashboardHandler`: markers for the **Live** map |
| GET | `/dashboard/presence/summary`, `/users` | Admin (scope) | `PresenceDashboardHandler` (no dashboard caller) |
| GET | `/dashboard/telemetry/summary` | Admin (scope only) | `TelemetryDashboardHandler.GetSummary` |
| GET | `/dashboard/rides` | Admin, intern, fleet | `DashboardRidesHandler.ListRides` |
| GET | `/dashboard/rides/{id}/events`, `/{id}/route` | Admin (scope and handler) | Ride timeline and stored route |
| GET | `/dashboard/notifications/analytics/variants`, `/analytics/types` | Admin (group role gate) | Notification delivery analytics |

## Time windows and delta math

Three different window builders and three different delta functions exist. They are not interchangeable.

| Surface | Window builder | Current window | Previous window | Delta function |
| --- | --- | --- | --- | --- |
| Community, fleet, cancellation, uncomplete KPIs | `DashboardService.calculateTimeRanges` | `now - d` to `now` | `now - 2d` to `now - d` | `calculateKPI` |
| Global KPI cards | `KpiService.GetGlobalKPIs` | Monday 00:00 UTC to `now` | Previous Monday to this Monday | `kpiDelta` |
| Live analytics | `SupplyDemandService.GetLiveAnalytics` | `now - p` to `now` for `24h`, `7d`, `30d`; 2020-01-01 to `now` for `all` | `now - 2p` to `now - p`; none for `all` | `computeKPIValue` |
| `/live/kpis` (no caller) | `SupplyDemandService.GetKPIs` | `now - 24h` to `now` | `now - 48h` to `now - 24h` | `computeKPIValue` |

`calculateTimeRanges` maps `last_24_hours`, `last_14_days`, and `last_30_days` to their durations. `all_time` returns 2020-01-01 UTC to `now`, with an equally long previous window that ends at 2020-01-01, so its previous values are always 0. Anything else becomes 7 days. The HTTP layer validates `range` with `requests.GetKPIsRequest.Validate` first, which accepts only empty, `last_24_hours`, `last_7_days`, `last_14_days`, `last_30_days`, and `all_time`, so an unknown value returns 400 before reaching it. The API default is `last_7_days`. The Rides **KPIs** tab, **Community** page, and fleet overview default to `last_30_days`.

`GetLiveAnalytics` rejects any `period` other than empty, `24h`, `7d`, `30d`, or `all` with 400. It sets `comparison_available` to false for `all`.

| Function | Value | Percentage change |
| --- | --- | --- |
| `calculateKPI` (`service/dashboard/dashboard.go`) | Current window | `delta / previous × 100` when previous is above 0. **100** when previous is 0 and current is above 0. Rounded to 2 decimals. |
| `kpiDelta` (`service/dashboard/kpi.go`) | All-time total | `delta / previous × 100` when previous is not 0, else 0. Not rounded. |
| `computeKPIValue` (`service/dashboard/supply_demand.go`) | Current window | `delta / previous × 100` when previous is not 0, else 0. Not rounded. |

Worked examples:

- Market KPIs, `last_7_days`, requested at 2026-09-25 15:00. Current window is 09-18 15:00 to 09-25 15:00. Previous is 09-11 15:00 to 09-18 15:00. Completes go from 24 to 30: delta 6, change 25.
- Market KPIs, cancels go from 0 to 3: delta 3, change 100 (not infinity, not 0).
- Live analytics, completes go from 0 to 3: delta 3, change 0. The **Live** page hides the percentage when `value == delta`, so it shows only +3.
- Global KPIs on a Tuesday at 12:00 UTC: this week has 1.5 days of signups (40) and last week has 7 days (100). Delta is -60 and change is -60, even if growth is flat.

## Community market KPIs

`GetMarketKPIs` runs `GetDashboardMarketMetrics` and `CountDashboardSearches` for both windows, plus `ListDashboardCommunityRideHistogram` for `ride_request_volume`. All ride filters use `request_time` in a half-open window.

| Field | SQL predicate on `Rides` (plus `community_id` and window) |
| --- | --- |
| `users` | `Users` in the community, not deleted, `profile_completed`, not `auth_v3_provisional`, not `is_test`, with `COALESCE(profile_completed_at, created_timestamp)` before the window end. A running total, not window-scoped. |
| `drivers` | Same user filter plus `DriverApplications.is_active` and `COALESCE(activated_at, created_timestamp)` before the window end. Running total. |
| `ride_requests` | `status <> 'requested'` |
| `confirmed_requests` | `confirm_time IS NOT NULL AND cancel_reason IS DISTINCT FROM 'superseded'` |
| `ride_completes` | `status IN ('complete','pending_rider_rating') AND cancel_phase IS NULL` |
| `ride_cancels` | `cancel_phase IS NOT NULL AND driver_id IS NOT NULL` |
| `ride_uncompletes` | Confirmed, not superseded, `driver_id IS NULL AND match_time IS NULL`, and one of the three uncomplete branches below |
| `ride_searches` | `COUNT(*)` of `LocationSearches` with `timestamp BETWEEN start AND end` |
| `completion_rate` | `confirmed_completes / confirmed_requests × 100`, where `confirmed_completes` is `confirmed_requests` intersected with `ride_completes`. 0 when there are no confirmed requests. |

The uncomplete branches, shared by market, fleet, and uncomplete queries:

```sql
status = 'no_drivers_available'
OR (cancel_phase IS NOT NULL AND driver_id IS NULL AND status <> 'requested')
OR (status NOT IN ('complete','pending_rider_rating','requested','no_drivers_available')
    AND cancel_phase IS NULL
    AND request_time < end_at - INTERVAL '1 hour')
```

`ride_request_volume` counts `status <> 'requested'` rides per hour from `generate_series(date_trunc('hour', start), date_trunc('hour', end), '1 hour')`. Empty hours return 0.

### Fleet KPIs

`GetFleetKPIs` uses `GetDashboardFleetMetrics`, which applies the same ride predicates filtered by `fleet_id` instead of `community_id`. It adds:

- `earnings`: `SUM(driver_payout)` over completed rides in the window.
- `qr_payment_count` and `qr_payment_earnings`: `QrPayments` for the fleet with `payment_status` null or not `unpaid`, windowed on `created_at`. Earnings are `SUM(amount_total) / 100.0`.
- `total_earnings`: `earnings + qr_payment_earnings`.
- `market_users`: the `users` count for the fleet's home community.

The fleet histogram (`ListDashboardFleetRideHistogram`) uses `BETWEEN` and does not exclude `status = 'requested'`, unlike the community histogram.

### Cancellation and uncomplete KPIs

Both use `BETWEEN start AND end` (closed interval), unlike the half-open market query.

| KPI | Formula |
| --- | --- |
| `total_cancellations` | Rides with `cancel_phase IS NOT NULL AND driver_id IS NOT NULL` |
| Rider, driver, system cancellation rate | `rider / total × 100`, `driver / total × 100`, `max(total - rider - driver, 0) / total × 100`. `rider` and `driver` come from `cancel_user`. |
| `avg_time_before_cancel` | `AVG(complete_time - request_time)` in seconds |
| `avg_time_after_match` | `AVG(complete_time - match_time)` in seconds |
| `total_uncompletes` | The uncomplete predicate above |
| `no_drivers_available_share`, `stale_in_flight_share` | Category count divided by total, times 100 |
| `avg_time_to_uncomplete` | `AVG(COALESCE(complete_time, end_at) - request_time)` |
| `avg_stale_age` | `AVG(end_at - request_time)` over stale rows only |

Example: 20 matched cancellations, 8 by riders and 5 by drivers. Rates are 40, 25, and 35 (system = 20 - 8 - 5 = 7).

Uncomplete rows are categorized by `ListDashboardUncompletes` and `ListDashboardUncompleteCategories` in this order: `no_drivers_available`, then pre-match cancels split into `rider_cancel`, `driver_cancel`, `match_timeout` (`cancel_reason = 'automatic'`), and `unknown_cancel`, then `stale_in_flight`, else `other`. Rows with no `complete_time` get `end_time = end_at` and `is_synthetic_end_time = true`.

Tables default to `limit` 50 with no upper bound (`parseTableParams`).

### Search insights

`GetSearchInsights` loads four datasets for the current window only:

1. Search counts per `LocationSearches.address`.
2. Ride counts per `Rides.destination_address` (all statuses, including `requested`).
3. Funnel counts: `requests` (`status <> 'requested'`) and `completes`.
4. The 100 most recent searches.

`buildLocationInsights` iterates search addresses only. Per-address conversion is `ride_count / search_count × 100`. Totals sum over search addresses, so rides to destinations nobody searched are dropped. The list is sorted by search count, then address, and cut to 10. `enrichRecentSearches` sets `search_type` to `zero_results` when latitude and longitude are both 0, and looks up rider names once per user (`Unknown rider` on error).

`search-map-data` returns up to 100 grouped points with non-empty address and non-zero coordinates, plus the community bounding box.

## Global KPIs

`GetGlobalTotals` in `data/repository/kpi.go` runs `GetGlobalKPITotals` three times: all-time (null window), this week, and last week. Every user-based subquery excludes deleted, incomplete, provisional, and test users, and users in test communities.

| Metric | All-time and windowed definition |
| --- | --- |
| `users` | `COUNT(*)` of qualifying users. Window on `COALESCE(ua.profile_completed_at, u.created_timestamp)`. |
| `drivers` | `COUNT(DISTINCT u.id)` with `da.is_active`. Window on `COALESCE(da.activated_at, u.created_timestamp)`. |
| `gmv` | `SUM(COALESCE(discounted_price, price))` for completed, uncanceled rides in non-test communities. Window on `request_time`. |
| `attenders` | `COUNT(DISTINCT ea.user_id)` from `EventAttendees`. Window on `ea.created_at`. |
| `rides` | `COUNT(*)` of completed, uncanceled rides. Window on `request_time`. |

`GMV` and `rides` filter out test communities but not test riders.

`ListWeeklyKPISeries` builds `kpiSeriesWeeks = 12` Monday buckets with `generate_series(date_trunc('week', NOW()) - 11 weeks, date_trunc('week', NOW()), '1 week')` and left-joins each metric grouped by `date_trunc('week', ...)`. Empty weeks come back as 0 from SQL. The current week is partial.

### Acquisition breakdown

`ListKPIAcquisitionChannels` resolves one source per qualifying user and counts by source. The first matching rule wins:

| Order | Evidence | Source |
| --- | --- | --- |
| 1 | Touch `landing_surface` like `%world%cup%` | `world_cup` |
| 2 | `landing_surface` or `referrer_path` contains `driver` | `driver_page` |
| 3 | Touch `referral_code` present | `invite_link` |
| 4 | `landing_surface` or `referrer_path` contains `join`, or `source_channel` contains `community` | `join_link` |
| 5 | `dub_click_id` present | `dub` |
| 6 | `utm_source` present | `utm:` plus the lowercased value |
| 7 | `source_channel` present | Lowercased channel |
| 8 | `landing_surface` in `web`, `/auth/v3` | `web` |
| 9 | `landing_surface` in `app`, `ios`, `android`, `native` | `app` |
| 10 | A `Referrals` row with the user as referee | `invite_link` |
| 11 | First non-empty `referral_sources.source` | `self_reported:` plus a slug |
| 12 | First verified or completed signin `AuthTransactions` row from 5 minutes before to 24 hours after signup | Its `channel` |
| 13 | `Users.acquisition_source` of `app_referral`, `app_organic`, `event_link`, or other | `invite_link`, `app`, `join_link`, or the raw value |
| 14 | Signup before the earliest `AttributionTouches.touched_at` | `legacy_unattributed` |
| 15 | None | `unknown` |

The touch is the user's `first_touch_id`, falling back to `last_touch_id`, then to the earliest touch owned by the user. `AttributionPending` is hardcoded to `false`.

### Manual KPI log and approvals

`CreateManualEntry` accepts `metric` in `revenue`, `spotlights`, `impressions`, requires `period_start`, and bounds `value` to 0 through `maxManualEntryValue = 10_000_000`. `created_by` is the Clerk user ID. Entries list by `period_start DESC, created_timestamp DESC`.

`GetGlobalApprovals` returns `GetGlobalPendingDriverApplications` unscoped. For rows in catch-all communities with non-zero signup coordinates, `resolveSignupCities` sets `SignupCity` to "City, ST" through the GTM geocode cache.

## Live operations

For the full request flow, polling, and presence behind the **Live** page, see [Live operations internals](/engineering/domains/live-operations).

The **Live** page and its API share the `supply_demand` handler package and `SupplyDemandService`. Every read is scoped with `resolveCommunityScope` (aggregates) or `resolveCommunityID` (market paths). The page always passes one `community_id`.

### Live analytics

`GetLiveAnalytics` (`service/dashboard/live.go`) makes four reads for a bounded period: `GetLiveRideCohorts` and `GetSupplyDemandKPIs` for the current window and again for the previous one. `all` makes two.

`GetLiveRideCohorts` (`queries/supply_demand.sql`) groups rides with `request_time` in `[start, end)` in non-test, ride-enabled communities by `status`, `cancel_phase`, `cancel_user`, and whether `confirm_time`, `match_time`, `at_pickup_time`, and `pickup_time` are set. It has no status filter, so unconfirmed `requested` rows are included. `aggregateLiveCohorts` (`repository/live.go`) derives:

| Field | Definition |
| --- | --- |
| `requests` | Every ride in the window |
| `completed` | `status IN ('complete','pending_rider_rating')` and no `cancel_phase` |
| `cancelled_by_rider`, `cancelled_by_driver` | Canceled rows by `cancel_user`. System cancels only appear in `flow`. |
| `unmatched` | `status = 'no_drivers_available'` without a `cancel_phase` |
| `failed_after_pickup` | Canceled rows with `pickup_time` set |
| `match_seconds` | Average of `match_time - confirm_time` where `match_time >= confirm_time`, with min and max |
| `pickup_seconds` | Average of `pickup_time - match_time` where `pickup_time >= match_time`, with min and max |
| `wait_seconds` | Average of `pickup_time - confirm_time` over rows that pass both checks, with min and max |
| `average_fare` | `SUM(COALESCE(discounted_price, price))` of completed rides divided by `completed` |
| `flow` | Sankey links. Each cohort walks `Request`, `Confirmed`, `Matched`, `Queue / en route`, `At pickup`, `In progress` as far as its timestamps go, then exits to `Completed`, `Unmatched`, `<Actor> canceled · <stage>`, or `Pending · <stage>`. |

Timing averages are `null` when no row qualifies.

`kpis` reuses `GetSupplyDemandKPIs` for `user_signups`, `driver_signups`, `gmv`, and `incompletes`, but replaces `completes` with the cohort `completed`. `completion_rate` is `completed / requests × 100` for each window, compared with `computeKPIValue`, so its `delta` is in percentage points.

| `GetSupplyDemandKPIs` field | Definition |
| --- | --- |
| `user_signups` | Qualifying users with `COALESCE(profile_completed_at, created_timestamp)` in the window |
| `driver_signups` | Active applications with `COALESCE(activated_at, created_timestamp)` in the window |
| `completes`, `gmv` | Same as global KPIs, windowed on `request_time` |
| `incompletes` | `no_drivers_available`, or canceled with no driver and not `requested`, or not in `complete`, `pending_rider_rating`, `requested` with no cancel and requested more than 1 hour before the window end |

`incompletes` is looser than the market `ride_uncompletes`. It does not require `confirm_time`, does not exclude `superseded`, and its stale branch does not require `driver_id IS NULL`. A ride stuck `in_progress` with a driver for more than an hour counts on **Live** but not on **Rides** > **KPIs**.

### Competitor prices

`GetLiveCompetitorPrices` (`service/dashboard/live_pricing.go`) accepts the same periods and asks PostHog's Query API for `ride_aggregator_prices_loaded` events in the window, scoped by `properties.community_id` and excluding `properties.is_test`. It averages `price_value` for options where the provider is `uber` with name `UberX`, or `lyft` with key `standard`, the price is above 0, `needs_connect` is false, and `error_kind` is empty.

| `status` | When |
| --- | --- |
| `not_configured` | `POSTHOG_PERSONAL_API_KEY` or `POSTHOG_PROJECT_ID` is empty, the project ID isn't numeric, or `POSTHOG_QUERY_HOST` isn't an `https` URL |
| `unavailable` | The request fails, returns non-200, or has an unexpected row shape |
| `ready` | Results parsed. Cached in memory for 5 minutes per scope and window length, up to 512 entries. |

The HTTP client times out after 12 seconds. Any valid `period` returns 200, whatever the status.

### Market summaries

`ListMarketSummaries` returns `Communities` rows that are `is_active` and not `rides_disabled`:

| Field | Definition |
| --- | --- |
| `active_rides` | Rides in `confirmed`, `queued`, `driver_en_route`, `driver_waiting`, `in_progress` with no `cancel_phase`, with no time bound |
| `unmatched_requests` | Last 24 hours, `driver_id IS NULL`, and `status = 'confirmed'` or non-superseded `no_drivers_available`. The **Live** page doesn't show it. |
| `last_driver_offline_at` | `MAX(DriverSessions.end_time)` for the market's drivers |
| `market_status`, `operating_windows`, `limited_hours_enabled`, `next_operating_window`, `time_zone`, `bounding_box` | `Community.GetByID(...)`, one call per market with at most six in flight. `OFF_PEAK` and no windows on error. |
| `drivers_online` | Members of `community:drivers:{id}` whose `presence:{id}` coordinates fall within the bounding box plus 10 km, or have no parseable coordinates. The SQL open-session count is discarded. |
| `riders_online` | `community:online:{id}` count minus the driver member count, floored at 0 |
| `presence_available` | True only when Redis presence was read. Otherwise `drivers_online` is 0 and `riders_online` is `null`. |

The service re-sorts by `active_rides` descending, then `LIVE` before `OFF_PEAK`, then `drivers_online` descending, then name.

### Market drivers and rides

`GetMarketDrivers` lists users in the market with an active application whose latest session is open, ended within 7 days, or whose `last_pinged_driver` is within 7 days. `fleet_name` joins the names of the driver's active `FleetDrivers` memberships, so a driver in two fleets is one row. Each row also carries `photo_url` (thumbnail first) and the stored `Users.latitude` and `longitude`. Each row is then enriched:

1. Status is `online` if the user is in `community:drivers:{id}` (scored in the last 120 seconds), `warm` if in `community:warm:{id}` (last 60 minutes, `warmPoolMaxAge`), else `offline`.
2. Online and warm drivers take `LastPingedDriver` from `presence:{id}.ts`. Warm and offline drivers get `SessionEndTime` set to the later of `last_pinged_driver` and `session_end_time`, ignoring timestamps before 2020.
3. `decline_reasons` are `RideDeclineReasons` since today's start.
4. Rows sort online, warm, offline, then `rides_today` descending, then last active.

Today starts at local midnight in the community's time zone, falling back to UTC when the zone doesn't load. `rides_today` counts `complete` and `pending_rider_rating` rides with `request_time` at or after that start. Tags prepend `EDU` for student accounts.

`GetMarketRides` returns non-`requested` rides from the last hour. With `older=true` the cutoff is 24 hours. Without it, `HasOlderMarketRides` sets `has_older` when rides exist between 24 hours and 1 hour ago. Rider and driver names are first name plus last initial. The **Live** page reads rides from `GET /dashboard/rides` instead.

### Surge

`SetSurgeRequest` validates `multiplier` 1 to 9.99 and `expires_in_hours` 1 to 168. The dashboard `SurgeSlider`, on **Live** and on **Pricing Management**, limits these to 1.0 to 3.0 and 1 to 4 hours.

<Steps>
  <Step title="Resolve the actor">
    `resolveClerkUser` loads the Clerk user for name and first email. On failure both fall back to the Clerk user ID.
  </Step>
  <Step title="Close the open history row">
    `CloseActiveSurgeHistory` sets `ended_at = NOW()` on rows with no `ended_at`, `expires_at` in the future, and `multiplier > 1`. `end_reason` is `manually_cleared` when the new multiplier is exactly 1.0, else `overwritten`. Failure is logged only.
  </Step>
  <Step title="Insert history and update the community">
    `InsertSurgeHistory` writes a row even for 1.0 (failure logged only). `SetCommunitySurge` writes `surge_multiplier`, `surge_expires_at`, and `surge_set_by`. This write is the only one that fails the request.
  </Step>
  <Step title="Invalidate quotes">
    `PriceCache.InvalidateCommunity` runs in a detached goroutine so it outlives the request.
  </Step>
</Steps>

Surge history lists 20 rows by default and falls back to 20 when `limit` is 0 or above 100. `deriveSurgeStatus` reports `clear_action` for 1.0 rows, then the stored `end_reason`, then `active` or `expired`.

### Notify and ping

No routed dashboard page calls these endpoints. `NotifyMarketDialog` and `PingDialog` in `yelo-web/src/components/live/` are only imported by the unrouted `DriverOpsPage`. `NotifyMarket` has no `requireAdmin` check; it relies on the scope middleware and `resolveCommunityID`. It calls `SendNotificationToCommunity` with the request `recipient` and always returns `sent_to: 1`. `PingDriver` and `PingRider` call `NotifyDashboardPingDriver` and `NotifyDashboardPingRider`. A send failure returns HTTP 200 with `sent: false`. Push delivery is covered in [Send pipeline](/engineering/notifications/send-pipeline).

## Driver ops snapshot

No routed dashboard page calls this endpoint; `/driver-ops` redirects to `/live`. `DriverOpsService.GetSnapshot` composes existing live operations calls. For N markets with D drivers it makes 1 summary query, N `Community.GetByID` calls, N driver lists, N ride lists, and per-driver Redis and Postgres reads.

| Field | Source |
| --- | --- |
| `markets` | `GetMarketSummaries(ctx, nil)`. A market that fails to build is logged and skipped. |
| `online_count` | Drivers with presence status `online` |
| `aq_on_count` | `presence:{id}` field `aq = "1"` via `GetAutoQueueFlag` |
| `founding_count` | Tags containing `Founding Driver`, `founding driver`, or `founding` |
| `no_activity_7d` | True when the market has drivers and none has `LastPingedDriver` or `SessionEndTime` after `now - 7 days` |
| `last_request` | First row of `GetMarketRides(..., false)` |
| `active_ride` (online only) | `GetDriverActiveRideInfo`. `since` is `pickup_time` if set, else `match_time`. |
| `queue` (online only) | `GetDriverQueuedRides`, empty array otherwise |
| `earnings` (online only) | `EarningsService.GetBalance` available and pending cents |
| `payout` (online only) | `DriverOpsPayoutReader.GetFoundingPayout`, nil when no reader is injected |

## Presence aggregation

Handlers build a `dashboard.CommunityScope`. Admins without `community_id` get `AllCommunities()`, which fans out over every community from `Community.GetAllLight`. Everyone else gets `OneCommunity` from their claim. The zero value matches nothing.

| Output | Computation per community |
| --- | --- |
| `online_users` | `ZCOUNT community:online:{id}` over the last 120 seconds |
| `online_drivers` | `ZCOUNT community:drivers:{id}` over the last 120 seconds |
| Screen, app state, network distributions | For each online member, `HGET presence:{userId}` fields `screen`, `app_state`, `network` |
| `warm_pool` | Members of `community:warm:{id}` scored in the last 60 minutes |
| `unmatched_rides` | `GetUnmatchedRidesForActivation`: `confirmed`, no driver, no live offer in the last 30 seconds, confirmed within `config.RideRequestExpiry` |

The **Live** page calls only `/presence/map`, every 10 seconds, with `community_id`. `/presence/summary` and `/presence/users` have no dashboard caller.

`/presence/users` builds the full list in memory (online first, then warm, deduplicated by user ID), slices `offset` and `limit` (default 50, negatives rejected with 400), then loads each page row with `User.GetByID`. `/presence/map` returns online markers from the presence hash and warm markers from `driver:loc:{userId}`. Warm drivers without a stored location are omitted from the map.

## Telemetry summary

`GetTelemetrySummary` in `data/repository/dashboard.go` runs one count and ten `ListDashboardTelemetryDistribution` calls sequentially under a shared 15-second timeout (`WithCustomTimeout`). Dimensions are `platform`, `app_version`, `os_version`, `notifications`, `location_permission`, `contact_permission`, `device_model`, `locale`, `carrier`, and `screen_size`.

- `notifications` maps `notifications_enabled` to `enabled` or `disabled`.
- `carrier` uses `COALESCE(carrier, 'unknown')`.
- `screen_size` is `screen_width × screen_height`.
- Only `app_version` receives `app_version_months`. It filters `dt.updated_at >= NOW() - make_interval(months => n)` when n is not 0.
- Without a community, test communities are excluded. With one, only that community's users count.

## Dashboard rides list

`ListRides` resolves scope, then `BuildDashboardRidesParams` and `GetDashboardRides`:

| Rule | Value |
| --- | --- |
| Admin with `all=true` | No community predicate; `community_name` is populated |
| Admin without `all` | `community_id` required, else 400 |
| Intern or fleet | `ResolveDashboardCommunity(community_id)`; `all` is ignored |
| Fleet | `FleetIDs` replaced by `GetFleetIDsForCommunity`; 403 if empty; `fleet_id` is ignored |
| `limit` | Default 50. Values below 1 or above 1000 become 50. With `include_routes=true` (admin only) capped at 200. |
| `sort` | Service allows `request_time`, `rider_name`, `driver_name`, `status`, `price`, `fleet_name`; others become `request_time` |
| `status` | `live`, `active`, `completed`, `canceled`; any other value means no filter. `live` is uncanceled `confirmed`, `queued`, `driver_en_route`, `driver_waiting`, `in_progress`, plus `requested` rows from the last 5 minutes. |
| `recent_only` | `true` limits rows and `total` to `request_time` in the last 24 hours |
| `search` | `ILIKE '%' + search + '%'` on rider name, driver name, or ride ID text |
| Routes | Encoded routes over 131072 bytes are skipped in SQL and in `dashboardMapRoute` |
| `has_active` | Any ride in scope with `status <> 'complete'` and no `cancel_phase`, unbounded in time |

`GetRideEvents` computes `time_to_match_ms` from the last `confirmed` event to the last `accepted`, `auto_queued`, or `direct_assigned` event, `time_to_pickup_ms` from that accept to the first `driver_at_pickup`, and `ride_duration_ms` from `rider_picked_up` to `completed`. Each event carries `elapsed_since_previous_ms`.

## Notification analytics

Both analytics endpoints accept `community_id`, `ride_id`, `user_id`, `variant_id`, `notification_type`, `from`, `to` (RFC 3339), `include_test`, `limit` (default 50, max 100), and `offset`.

| Field | Formula |
| --- | --- |
| `allocated_count` | Deliveries in the variant's cohorts |
| `attempted_count` | Status `sent`, `delivered`, or `failed` |
| `accepted_rate` | `(sent + delivered) / attempted` |
| `tap_rate` | `opened / delivered` |
| `deliverability_rate` | `delivered / (delivered + failed)`, only once `MAX(sent_at) + 15 minutes` has passed |
| `average_dispatches_per_day` (types) | `COUNT(DISTINCT n.id) / GREATEST((MAX(created_at) - MIN(created_at)) in days, 1)` |

Ratios are `null` when the denominator is 0. Test notifications and test deliveries are excluded unless `include_test=true`.

## GTM snapshot and refresh job

`GtmService` reads catch-all prospects: users in communities where `is_default = TRUE OR is_active = FALSE`, with completed, non-provisional, non-test profiles and non-zero coordinates (`ListGTMCatchAllUsers`).

| Item | Value |
| --- | --- |
| River kind | `gtm_refresh` on queue `gtm` (`MaxWorkers: 1`) |
| Schedule | `river.PeriodicInterval(5*time.Minute)`, `RunOnStart: true`, skipped when `deps.GTM` is nil |
| Uniqueness | `ByArgs` across available, pending, running, retryable, scheduled states |
| Job timeout | 2 minutes (`gtmRefreshWorker.Timeout`) |
| Snapshot key | `gtm:resolved`, JSON, TTL 24 hours |
| Geocode key | `gtm:city:{lat}:{lng}` rounded to 2 decimals, city and state joined by a pipe, TTL 24 hours |
| Read timeout | 2 seconds; a missing or corrupt snapshot returns `ErrorTypeProviderUnavailable` |

`Refresh` geocodes every user through the per-coordinate cache. Any Mapbox error aborts the run and keeps the previous snapshot. Cached coordinates keep progress for the retry. Users whose city resolves empty are dropped.

`aggregateGtmCities` groups by city name. `wow_pct` is `signups in the last 7 days / signups before that`, a ratio rather than a percentage change, and is `null` when no earlier signups exist. Example: 3 recent and 12 older signups give `wow_pct = 0.25`. CSV export prefixes cells that start with `=`, `+`, `-`, `@`, tab, CR, or LF with a quote.

See [Background jobs](/engineering/api/background-jobs) for the full periodic job table.

## Caching summary

| Surface | Cache |
| --- | --- |
| KPI, market, fleet, cancellation, search, telemetry, rides | None. Every request queries Postgres with a 5-second `WithPostgresTimeout`, except telemetry at 15 seconds. |
| Presence, market online counts, market driver status, AQ flag | Live Redis reads, no memoization |
| Competitor quotes | In-process map, 5 minutes per scope and window length, cleared at 512 entries |
| GTM | Redis snapshot and geocode cache, 24-hour TTL, refreshed every 5 minutes |
| Surge | Writes invalidate the community price cache asynchronously |
| User detail edits | Invalidate the user profile cache (best effort) |

## Where this lives in code

| Concern | File | Function or type |
| --- | --- | --- |
| Market, fleet, cancel, uncomplete, search KPIs | `internal/service/dashboard/dashboard.go` | `GetMarketKPIs`, `GetFleetKPIs`, `GetCancellationKPIs`, `GetUncompleteKPIs`, `GetSearchInsights`, `calculateKPI`, `calculateTimeRanges` |
| KPI SQL | `internal/data/repository/queries/analytics.sql` | `GetDashboardMarketMetrics`, `GetDashboardFleetMetrics`, `GetDashboardUncompleteMetrics`, `ListDashboardUncompletes` |
| Global KPIs | `internal/service/dashboard/kpi.go`, `internal/data/repository/kpi.go` | `GetGlobalKPIs`, `kpiDelta`, `GetGlobalTotals`, `GetWeeklySeries` |
| Global KPI SQL | `queries/analytics.sql` | `GetGlobalKPITotals`, `ListWeeklyKPISeries`, `ListKPIAcquisitionChannels` |
| KPI routes | `internal/server/http/dashboard/kpi/router.go`, `handlers.go` | `RegisterRoutes`, `requireAdmin` |
| Live operations | `internal/service/dashboard/supply_demand.go`, `live.go`, `live_pricing.go`, `queries/supply_demand.sql`, `data/repository/live.go` | `GetMarketSummaries`, `populateLivePresence`, `GetMarketDrivers`, `SetSurge`, `GetLiveAnalytics`, `aggregateLiveCohorts`, `GetLiveCompetitorPrices` |
| Live routes | `internal/server/http/dashboard/supply_demand/router.go`, `handlers.go`; mounted at `/dashboard/live` in `server/http/router.go` | `resolveCommunityScope`, `resolveCommunityID`, `requireAdmin` |
| Driver ops | `internal/service/dashboard/driver_ops.go` | `GetSnapshot`, `buildMarket`, `buildDriver` |
| Presence | `internal/service/dashboard/presence.go`, `server/http/dashboard/presence/presence_handler.go` | `CommunityScope`, `GetPresenceSummary`, `resolveCommunityScope` |
| Telemetry | `internal/data/repository/dashboard.go` | `GetTelemetrySummary` |
| Rides | `internal/service/dashboard/rides.go`, `data/repository/dashboard_rides.go` | `BuildDashboardRidesParams`, `GetDashboardRides`, `GetRideEvents` |
| Notification analytics | `internal/data/repository/notification.go`, `queries/notification_log.sql` | `GetNotificationVariantAnalytics`, `GetNotificationTypeAnalytics` |
| GTM | `internal/service/dashboard/gtm.go`, `internal/notify/queue/gtm.go`, `queue.go` | `Refresh`, `gtmRefreshWorker`, `gtmRefreshOptions` |
| Zone pricing backtest | `internal/service/dashboard/zone_pricing.go`, `models/backtest.go` | `SimulateZonePricing` (limit default 100, max 500), `ZonePricingBacktestRow` |

## Gotchas and edge cases

- **Rides sort options are silently dropped.** `GetDashboardRides` (`service/dashboard/rides.go`) rewrites any `sort` outside its six fields to `request_time`, even though `resolveDashboardRideSortField` and the SQL support `time_to_match`, `time_to_pickup`, `ttd`, `wait`, `time`, and `ride_distance`.
- **`active` and `completed` overlap.** The rides `active` filter includes `pending_rider_rating`, which `completed` also includes. `active` also includes `requested` and `no_drivers_available`.
- **Rides search treats `%` and `_` as wildcards.** `GetDashboardRides` (`data/repository/dashboard_rides.go`) does not escape them, unlike the user directory search.
- **`has_active` never expires.** `HasActiveDashboardRides` counts any uncanceled ride that isn't `complete`, with no time bound, so stale `no_drivers_available` or `pending_rider_rating` rows keep the Trips tab on its 10-second poll. Every `GET /dashboard/rides` call runs it, including the two **Live** ride polls.
- **Live and Rides use different completion math.** **Live** `completion_rate` divides by every request, including unconfirmed `requested` rows. Market `completion_rate` divides by confirmed, non-superseded requests. **Live** `incompletes` is also looser than market `ride_uncompletes`.
- **Zero-baseline percentages disagree.** `computeKPIValue` returns 0 when the previous value is 0, while `calculateKPI` returns 100. The **Live** page hides the percentage in that case; other pages show 100%.
- **`all_time` comparisons are empty.** `calculateTimeRanges` gives `all_time` a previous window that ends at 2020-01-01, so every non-zero KPI shows +100% on the Rides **KPIs** tab, **Community** page, and fleet overview.
- **Three definitions of unmatched.** `unmatched_requests` on market summaries counts `confirmed` plus non-superseded `no_drivers_available` over 24 hours. Live analytics `unmatched` counts only `no_drivers_available` in the period. Presence counts only `confirmed` within the request expiry.
- **`active_rides` has no time bound.** A ride stuck in `confirmed` or `in_progress` counts on its market card indefinitely, while the **Live** rides list drops unconfirmed requests after 5 minutes.
- **Competitor quotes serialize.** `liveQuoteClient.prices` holds its mutex across the PostHog request, so concurrent cache misses wait on each other for up to 12 seconds each.
- **"Last req" is blank after an hour.** Driver ops calls `GetMarketRides` without `older`, so only rides from the last hour qualify.
- **`no_activity_7d` rarely fires.** `ListMarketDrivers` already drops drivers inactive for 7 days, so the flag is true only when every listed driver has an open session and no valid heartbeat.
- **Warm pool totals double count.** `countWarmPoolDrivers` does not remove online drivers, while `/presence/users` does.
- **Reads mutate Redis.** `GetWarmPoolDrivers` runs `ZREMRANGEBYSCORE` on every call, so the 10-second **Live** presence map and market driver polls prune the warm pool.
- **Warm users always look fresh.** `collectWarmUsers` sets `LastSeen` to `time.Now()`.
- **Invalid telemetry community means global.** `GetSummary` ignores an unparseable `community_id`.
- **GTM has no HTTP surface.** The `GtmService` comment points to `server/http/dashboard/gtm`, which does not exist. `GetCities`, `GetCityDrivers`, `GetCityCSV`, and `GetPoints` have no callers. The 5-minute refresh still geocodes every prospect, and its only consumer is the approvals city label via the shared `gtm:city:*` cache.
- **Manual KPI log and approvals are API-only.** The generated client has the operations, but no dashboard page calls them.
- **Series comment is stale.** `GetWeeklySeries` says the service back-fills empty weeks. The SQL already does it; the service does not.
- **Pricing previews apply expired surge.** Rider quotes in `service/ride/rider.go` apply `surge_multiplier` only while `surge_expires_at` is in the future. The dashboard pricing overview (`surgeFor` in `service/dashboard/pricing_overview.go`) and pricing preview (`GetCommunitySurgeMultiplier` in `service/dashboard/pricing_preview.go`) ignore `surge_expires_at`, so after a surge expires they keep showing surged prices that riders no longer pay.
- **Market notify always reports one.** `NotifyMarket` returns `sent_to: 1` regardless of recipients.
- **Mixed interval types.** Market and fleet metrics use half-open windows; cancellation, uncomplete, and search queries use `BETWEEN`. A ride exactly on the boundary can count in both windows.
